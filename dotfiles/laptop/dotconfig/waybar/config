// waybar laptop config

{
  // include the common config
  "include": "~/.config/waybar/common",

  // choose the order of the modules
  "modules-right": ["mpris", "network", "custom/kernel", "custom/cpu", "custom/gpu", "custom/memory", "custom/DualSense", "battery"],
}
  
