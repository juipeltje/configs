// waybar workstation config

{
  // include the common config
  "include": "~/.config/waybar/common",

  // choose the order of the modules
  "modules-right": ["mpris", "custom/os-version", "custom/kernel", "custom/cpu", "custom/gpu", "custom/water", "custom/pump", "custom/fans", 
                      "custom/memory", "custom/DualSense"],
}
  
